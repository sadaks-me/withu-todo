name: withu_todo
description: A Todo Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^2.1.0
  firebase_core: ^1.1.1
  cupertino_icons: ^1.0.3
  flutter_slidable: ^0.6.0
  provider: ^5.0.0
  get: ^4.1.4
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.16.5
  build_runner: ^2.0.2
  json_annotation: ^4.0.1
  json_serializable: ^4.1.1

flutter:

  uses-material-design: true
